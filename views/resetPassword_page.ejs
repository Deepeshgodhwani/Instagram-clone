<!DOCTYPE html>
<html>
  <head>
    <style>
      #container {
        width: 100%;
        height: 80vh;
        padding-top: 90px;
        background-color: rgb(250, 250, 250);
        display: flex;
        justify-content: center;
        /* align-items: center ; */
      }
      .box {
        margin-top: 40px;
        width: 20rem;
        height: 60vh;
        border: 1px solid rgb(219, 219, 219);
        background-color: white;
        display: flex;
        flex-direction: column;
        /* row-gap: 1rem; */
        padding: 1rem;
      }
      .profile {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        margin-top: 20px;
      }

      .profile p {
        margin-top: 10px;
        font-weight: 500;
        font-family: calibri;
        font-size: large;
        margin-left: 10px;
      }
      #dp {
        width: 70px;
        border-radius: 50%;
      }
      .error {
        color: rgb(237, 73, 86);
        font-weight: 100;
        font-family: calibri;
        text-align: center;
        width: 85%;
        margin-left: 20px;
        margin-top: -5px;
        margin-bottom: -10px;
      }

      #new {
        background-color: rgb(250, 250, 250);
        border: 1px solid rgb(219, 219, 219);
        width: 90%;
        margin-left: 8px;
        height: 1.7rem;
        padding: 5px;
        outline: none;
        border-radius: 3px;
        margin-bottom: 15px;
        margin-top: 0px;
        font-size: 0.7rem;
      }
      .button {
        width: 93%;
        height: 2.7rem;
        margin-left: 8px;
        border-radius: 5px;
        border: none;
        margin-top: 10px;
        outline: none;
        background-color: rgb(0, 149, 246);
        color: white;
        font-weight: 600;
      }
    </style>
  </head>
  <body>
    <%- include('header'); -%>
    <div id="container">
      <div class="box">
        <div class="profile">
          <img id="dp" src="<%=User.avtar%>" />
          <p><%=User.username%></p>
        </div>
        <%if(error){%>
        <p class="error"><%=error.message%></p>
        <p></p>
        <%}%>
        <form action="/account/reset?id=<%=User._id%>" method="post">
          <input
            autocomplete="off"
            id="new"
            type="password"
            name="password"
            placeholder="New password"
            minlength="8"
            maxlength="20"
            required
          />
          <input
            autocomplete="off"
            id="new"
            type="password"
            name="confirm_password"
            minlength="8"
            maxlength="20"
            placeholder="Confirm new password"
            required
          />
          <a
            ><input
              class="button"
              type="submit"
              value="Change Password"
              required
          /></a>
        </form>
      </div>
    </div>
  </body>
</html>
